version: "3"

services:
  db:
    container_name: baserow-demo-db
    image: postgres:11.3
    environment:
      - POSTGRES_USER=baserow
      - POSTGRES_PASSWORD=baserow
      - POSTGRES_DB=baserow
    networks:
      - baserow_demo_backend

  redis:
    container_name: redis
    image: redis:6.0
    networks:
      - baserow_demo_backend

  mjml:
    container_name: baserow-demo-mjml
    image: liminspace/mjml-tcpserver:latest
    networks:
      - baserow_demo_backend

  backend:
    container_name: baserow-demo-backend
    build:
      context: ./backend/
      dockerfile: Dockerfile.demo
    ports:
      - 8000:8000
    depends_on:
      - db
      - redis
      - mjml
    networks:
      - baserow_demo_backend
      - baserow_demo_frontend

  web-frontend:
    container_name: baserow-demo-web-frontend
    build:
      context: ./web-frontend/
      dockerfile: Dockerfile.demo
    ports:
      - 3000:3000
    depends_on:
      - backend
    networks:
      - baserow_demo_frontend
      - baserow_demo_backend

networks:
  baserow_demo_backend:
    driver: bridge
  baserow_demo_frontend:
    driver: bridge
